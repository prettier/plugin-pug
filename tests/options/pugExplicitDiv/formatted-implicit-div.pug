#id.class(attribute="value") div with id attr test
.class(attribute="value" attr2) div with class attr test
#id.class(attribute="value") div with id and class test
#id#litId.litClass.class(attribute="value") div with literals and atters id & class test
#attrId#myId.myClass.attrClass no div with literals and attrs
#litId(attr="value")
#srcIdSecond.srcClassFirst(attr="value") even more text
#srcIdFirst.srcClassSecond
.justClass
#justId
.wrapper(attr="78 char input line, will be 81 with div" my-props="[1, 2, 3]")
#longLine.test4wrapping(
  attr="there is a lot of text here, it should wrap it up"
  data-text="even more text here"
)
