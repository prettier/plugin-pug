div
  v-progress-circular(
    :color="getProgressColor(currentCapacity)",
    :size="circleSize",
    :value="capacityPercentage",
    :width="circleWidth"
  ) {{ currentCapacity }}/{{ maxCapacity }}
