each [key, val] of users
  li= key
  li= val

each data of users
  li= data[0]
  li= data[1]

div
  each [key, val] of users
    li= key
    li= val

  each data of users
    li= data[0]
    li= data[1]

each [key, val] of { a: "1", b: "2" }
